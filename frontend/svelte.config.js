/** @type {import('@sveltejs/kit').Config} */
const config = {
  // No kit configuration needed for basic Vite+Svelte setup
  // Just enabling TypeScript preprocessing
  compilerOptions: {
    dev: true
  }
};

export default config;